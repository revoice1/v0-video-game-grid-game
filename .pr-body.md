## Summary
- migrate search, validation, and puzzle generation from RAWG to IGDB
- improve puzzle quality, validation feedback, and per-cell difficulty hints
- polish gameplay UX with duplicate prevention, metadata modal, practice persistence, and SEO/domain updates

## What changed
- added a new IGDB client and removed the old RAWG integration
- reworked puzzle generation with validation thresholds, retry pacing, fallback handling, and cell difficulty metadata
- improved search filtering for official titles and added typo-tolerant fallback ranking
- added guessed-tile metadata modal, per-cell difficulty badges/counts, practice persistence, and a practice-only new game action
- updated site metadata, robots, sitemap, manifest, and canonical URLs for gamegrid.games

## Verification
- `npx tsc --noEmit`
- `npm run build`
